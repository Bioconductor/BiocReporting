% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_summary.R
\name{llm_summary}
\alias{llm_summary}
\alias{print.llm_summary}
\title{Generate a Summary of Commit Logs Using a Language Model}
\usage{
llm_summary(commits_log, llm = "gemini")

\method{print}{llm_summary}(x, ...)
}
\arguments{
\item{commits_log}{\code{character()} A character vector containing commit
messages. If multiple commit messages are provided, they will be
concatenated into a single string.}

\item{llm}{\code{character(1)} A string specifying the language model to
use. Currently, only "gemini" is supported. Default is "gemini".}

\item{x}{an object used to select a method.}

\item{...}{further arguments passed to or from other methods.}
}
\value{
The function prints the summary generated by the language model.
}
\description{
This function takes a log of commits and generates a concise summary using a
specified language model. The summary is intended to report the progress of
several projects to a funding agency.
}
\details{
The function uses a specified language model to create a summary of
the provided commit messages. The summary is intended to be suitable for
reporting to a funding agency. If the \code{commits_log} contains multiple
messages, they will be concatenated into a single string before being
passed to the language model.
}
\examples{
\dontshow{if ((interactive())) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
commits <- c(
    "Initial commit of project alpha",
    "Added new feature",
    "Fixed bug with new feature"
)
llm_summary(commits)
\dontshow{\}) # examplesIf}
}
